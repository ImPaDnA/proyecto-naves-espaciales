extends CanvasLayer

#func _ready():
#OS.has_feature() le pregunta al motor si estamos en un entorno móvil
	#if OS.has_feature("mobile"):
	#	show() # Muestra los botones en el celular
	#else:
	#	hide() # Los esconde si juegas en tu Linux Mint
